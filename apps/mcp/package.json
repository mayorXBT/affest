{
  "name": "@affest/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "stdio": "node dist/stdio.js",
    "dev": "tsc -p tsconfig.build.json && node dist/server.js"
  },
  "dependencies": {
    "@affest/application": "workspace:*",
    "@affest/chain-config": "workspace:*",
    "@affest/strategy-engine": "workspace:*",
    "@modelcontextprotocol/sdk": "1.30.0",
    "pg": "8.16.3",
    "viem": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "@types/pg": "8.15.5"
  }
}
