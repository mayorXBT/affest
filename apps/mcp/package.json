{
  "name": "@affest/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "stdio": "node dist/stdio.js",
    "dev": "tsc -p tsconfig.build.json && node dist/server.js"
  },
  "dependencies": {
    "@affest/application": "workspace:*",
    "@affest/chain-config": "workspace:*",
    "@modelcontextprotocol/sdk": "1.30.0",
    "viem": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "24.3.0"
  }
}
